const state = {

}

const mutations = {
  setTVDBAPIKey (state, payload) {
  }
}

const getters = {
  loginInfoIsPresent: state => {
    return
  }
}

const actions = {
  getLocalData ({ commit }) {
    return
  }
}

export default {
  state,
  mutations,
  actions,
  getters
}
